import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frontendreact.project',
  appName: 'frontend-react',
  webDir: 'build'
};

export default config;
